---
- name: Apply users - check mode
  opentelekomcloud.gitcontrol.members:
    root: "{{ root }}"
    token: "{{ token }}"
  check_mode: true

- name: Apply users
  opentelekomcloud.gitcontrol.members:
    root: "{{ root }}"
    token: "{{ token }}"
